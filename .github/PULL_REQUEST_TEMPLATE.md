## What does this change?

<!-- What, and why — link an issue if there is one. -->

## Testing

- [ ] `gradlew.bat :core:test :tools:simulator:test :android:app:assembleDebug` passes locally
- [ ] Added/updated a `core` unit test or `tools/simulator` scenario for any mesh-logic change
- [ ] Manually verified on-device (if touching `android/app`) — see [docs/TESTING.md](../docs/TESTING.md)

## Related

Closes #
